pipeline {
  agent {
    docker {
      image 'akakad/apache:latest'
    }
  }
  stages {
    stage('Build') {
      steps {
        sh 'cat /etc/lsb-release'
      }
    }
    stage(''Deploye) {
      steps {
        sh 'cat /etc/lsb-release' 
      }
    }
  }
}
